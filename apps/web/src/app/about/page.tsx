import { Layout } from "@components/Layout";
import React, { ReactElement } from "react";

const About = (): ReactElement => {
  return (
    <Layout>
      <h1>Example Page: About</h1>
    </Layout>
  );
};

export default About;
