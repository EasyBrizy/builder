import { Init as Control } from "./_components/Init";
import { Layout } from "@components/Layout";
import React, { ReactElement } from "react";

const Init = (): ReactElement => {
  return (
    <Layout>
      <Control />
    </Layout>
  );
};

export default Init;
