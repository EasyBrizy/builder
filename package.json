{
  "name": "brizy-preview",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "turbo": "^1.9.7"
  },
  "packageManager": "npm@9.5.1",
  "workspaces": [
    "examples/*",
    "packages/*"
  ]
}
